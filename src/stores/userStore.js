import { defineStore } from 'pinia';

export const useUserStore = defineStore('route', {
    state: () => ({
        user: {
            name:'' //Username
        }
    }),
    actions: {
        
    }

    // getters
})